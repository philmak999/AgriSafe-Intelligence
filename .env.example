# Free API key from https://console.groq.com/keys
GROQ_API_KEY=

# Optional — defaults to a Groq model with tool-calling support
GROQ_MODEL=llama-3.3-70b-versatile

# Optional — port for the local API server
PORT=8787
